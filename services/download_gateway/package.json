{
  "name": "hope-tv-download-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Deprecated R2 download Worker scaffold; production uses GitHub Releases.",
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test src/auth.test.ts src/handler.test.ts",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "echo \"Deploy only after OWNER_CONFIG and secrets are filled.\" && exit 1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "typescript": "^5.8.2",
    "wrangler": "^4.0.0"
  }
}
